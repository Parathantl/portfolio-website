# Application
NODE_ENV=development
PORT=3001
APP_URL=http://localhost:3001

# CORS - Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
FRONTEND_URL=http://localhost:3000

# Database (Development - Individual fields)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_DATABASE=blog
DB_SYNCHRONIZE=true

# Database (Production - Railway provides this single URL)
# DATABASE_URL=postgresql://user:password@host:port/database

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
CONTACT_EMAIL=your-contact-email@example.com

# Gemini AI (for AI-generated excerpts)
GEMINI_API_KEY=your-gemini-api-key

# File Upload
MAX_FILE_SIZE=5242880

# Storage Provider (local, cloudinary, s3, backblaze)
STORAGE_PROVIDER=local

# Cloudinary Configuration (if using Cloudinary)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
